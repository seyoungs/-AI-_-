-- 이 스크립트가 들어있는 도구를 변수에 저장해요
local tool = ______._____

-- 클라이언트로부터 신호를 받을 'PullEvent' 통로를 기다려서 가져와요
local pullEvent = tool:WaitForChild(_____)

-- 클라이언트가 신호를 보내면, 보낸 사람(player)과 클릭한 위치(targetPosition)를 받아와요
pullEvent.OnServerEvent:Connect(function(player, targetPosition)

	-- 클릭한 위치를 기준으로 반경 30 범위 안에 있는 모든 파트를 가져와요
	for _, part in ipairs(workspace:GetPartBoundsInRadius(_____, 30)) do

		-- 파트가 공중에 떠 있고(고정되어 있지 않고), 내 캐릭터 몸이 아닐 때만 실행해요
		if not part._____ and not part:IsDescendantOf(player.Character) then

			-- 클릭한 위치를 향해 파트를 튕겨나오게 힘(속도)을 줘요
			part.AssemblyLinearVelocity = (____________ - part.Position).Unit * _____
		end
	end
end)
